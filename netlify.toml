[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — required for React Router paths (/signup, /login, /verify, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
